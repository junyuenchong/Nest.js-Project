import { Config } from 'postcss-load-config';

// PostCSS Configuration for Tailwind CSS
const config: Config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config; 